export class ReceitaCategoria {
    valor: number;
    nomeCategoria: string;
    constructor(
            valor: number,
            nomeCategoria: string,
    ){
        this.valor = valor;
        this.nomeCategoria = nomeCategoria;
    }
}